services:
  - type: web
    name: emo-robot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: 4669cb47ef464621a16805cd6d9dbf5c
        sync: false   # you set this manually in Render dashboard
